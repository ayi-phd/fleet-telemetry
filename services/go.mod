module github.com/example/fleet-telemetry

go 1.24

// Dependencies are resolved by `go mod tidy` during the Docker build (see Dockerfile).
// Run `make deps` once with network access to pin versions and commit go.sum.
